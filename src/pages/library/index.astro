---
import Layout from '../../components/Layout.astro';
import { Card, LinkCard, CardGrid } from '@astrojs/starlight/components';
import {
  Pagination,
  PaginationContent,
  PaginationEllipsis,
  PaginationItem,
  PaginationLink,
  PaginationNext,
  PaginationPrevious,
} from "../../components/ui/pagination"


// const allItems = Object.values(await import.meta.glob('./earth/*.md'));
import { getCollection } from 'astro:content';
const allItems = await getCollection('library');

---

<Layout pageTitle='library' pageStyle='leftside' includes_p5 starlightSettings={{next: false, prev: false}}>

<CardGrid>
    {  allItems.map((item:any) =>
      <LinkCard title={item.data.title} href={'/library/' + item.id} />
      )
    }
 </CardGrid> 
  
<Pagination>
pagination not working yet, bear with me
  <PaginationContent>
    <PaginationItem>
      <PaginationPrevious href="#" />
    </PaginationItem>
    <PaginationItem>
      <PaginationLink isActive href="#">1</PaginationLink>
    </PaginationItem>
    <PaginationItem>
      <PaginationEllipsis />
    </PaginationItem>
    <PaginationItem>
      <PaginationNext href="#" />
    </PaginationItem>  
  </PaginationContent>
</Pagination>

</Layout>